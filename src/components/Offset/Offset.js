import React from "react";
import "../Offset/Offset.css";

const Offset = () => {
  return (
    <div className="container">
      <div className="row">
        <div className="col-md-5 offset-md-3 offset-lg-1  orange">
          Redux is an open-source JavaScript library used for managing the state
          of an application. It is commonly used with libraries like React or
          Angular for building user interfaces. Redux follows the principles of
          Flux architecture, providing a predictable state container that helps
          to manage the state of an entire application in a single immutable
          state tre
        </div>
        <div className="col-md-3  green">
          Flux architecture, providing a predictable state container that helps
          to manage the state of an entire application in a single immutable
          state tre
        </div>
      </div>
    </div>
  );
};

export default Offset;
